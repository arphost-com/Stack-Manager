# Compose Manager Configuration

# Required: API key for authentication
API_KEY=change-me-to-a-secure-key

# Bootstrap web login. If ADMIN_PASSWORD is unset, the first admin password
# defaults to API_KEY when users.json does not exist.
ADMIN_USERNAME=admin
# ADMIN_PASSWORD=change-me-to-a-different-secure-password

# Docker projects root directory on the host
DOCKER_ROOT=/home/debian/docker

# Compose Manager persistent state directory on the host
STATE_DIR=/home/debian/.compose-manager

# Server runs non-root by default. Set DOCKER_GID to the host docker socket group:
# stat -c '%g' /var/run/docker.sock
DOCKER_GID=998

# Optional override for hosts that intentionally run compose management as root
# SERVER_USER=0:0
SERVER_USER=1001:1001

# Web UI host port
WEB_PORT=8193
